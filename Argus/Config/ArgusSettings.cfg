// GameData/Argus/Config/ArgusSettings.cfg
ARGUS_SETTINGS
{
  requireTrackingStationLevel = 2
  defaultDeploymentFee = 5000

  // Multipliers per body (fallback 1.0)
  BODY_FACTOR
  {
    body = Sun
    factor = 2.0
  }
  BODY_FACTOR { body = Kerbin  factor = 1.0 }
  BODY_FACTOR { body = Mun     factor = 1.2 }
  BODY_FACTOR { body = Minmus  factor = 1.2 }
  BODY_FACTOR { body = Duna    factor = 2.0 }
  BODY_FACTOR { body = Eve     factor = 2.5 }
  BODY_FACTOR { body = Jool    factor = 3.0 }
}